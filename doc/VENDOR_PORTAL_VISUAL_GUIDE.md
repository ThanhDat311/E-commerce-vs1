# Vendor Portal Architecture - Visual Guide

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     E-COMMERCE PLATFORM                      │
└─────────────────────────────────────────────────────────────┘
                              │
                    ┌─────────┴─────────┐
                    │                   │
          ┌─────────▼──────┐   ┌────────▼─────────┐
          │  Admin Portal  │   │  Vendor Portal   │
          │  (/admin/*)    │   │  (/vendor/*)     │
          └────────┬───────┘   └────────┬─────────┘
                   │                    │
        ┌──────────┴──────────┐        │
        │                     │        │
  Admin Sidebar        Admin Dashboard  │
  - Users             - All Products    │
  - Reports           - All Orders      │
  - Categories        - All Customers   │
  - Risk Rules        - Reports         │
  - Audit Logs        - System Stats    │
                                        │
                      ┌─────────────────┘
                      │
          ┌───────────▼──────────┐
          │  Vendor Sidebar      │
          │ - Dashboard          │
          │ - My Products        │
          │ - Orders             │
          │ - Profile            │
          └───────────┬──────────┘
                      │
          ┌───────────▼──────────┐
          │ Vendor Dashboard     │
          │ - My Stats           │
          │ - My Products        │
          │ - My Orders          │
          └──────────────────────┘
```

---

## 🔐 Authorization Flow

```
┌─────────────────────────────────────┐
│    User Visits /vendor/products     │
└─────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────┐
│ 1. Route Middleware Check               │
│ middleware(['auth', 'role:vendor'])    │
│                                         │
│ Is authenticated? ✅                    │
│ Is Vendor (role_id=4)? ✅              │
└──────────────────────────────────────────┘
                │ YES
                ▼
┌──────────────────────────────────────────┐
│ 2. Controller Action Executed            │
│ ProductController@index()               │
└──────────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────┐
│ 3. Query with VendorScope Applied       │
│ Product::all()                          │
│                                         │
│ WHERE vendor_id = auth()->user()->id   │
│                                         │
│ Returns: Only vendor's products        │
└──────────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────┐
│ 4. Return View with Filtered Data       │
│ resources/views/vendor/products/      │
│ index.blade.php                       │
└──────────────────────────────────────────┘
```

---

## 🚫 Authorization Denied Flow

```
┌─────────────────────────────────────────────┐
│   Vendor A tries to access /admin/users    │
└─────────────────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────────┐
│ Route Middleware: middleware(['role:admin'])│
│                                            │
│ Is Vendor (role_id=4)? ❌                 │
└─────────────────────────────────────────────┘
                │ NO
                ▼
┌──────────────────────────────────────────────┐
│ Abort with 403 Forbidden                    │
│ "Access denied"                             │
└──────────────────────────────────────────────┘
```

---

## 📊 Data Isolation Example

```
Database: products table
┌────┬────────────┬───────────┬────────────┐
│ id │ name       │ vendor_id │ price      │
├────┼────────────┼───────────┼────────────┤
│ 1  │ Product A  │ 10        │ $100.00    │
│ 2  │ Product B  │ 10        │ $150.00    │
│ 3  │ Product C  │ 11        │ $200.00    │
│ 4  │ Product D  │ 11        │ $250.00    │
│ 5  │ Product E  │ 12        │ $300.00    │
└────┴────────────┴───────────┴────────────┘

Auth::login(User::find(10)) // Vendor 10

Product::all()
    ↓
Apply VendorScope
    ↓
WHERE vendor_id = 10
    ↓
Returns: [Product A (id=1), Product B (id=2)]

Vendor 10 sees: 2 products
Vendor 11 sees: 2 products
Vendor 12 sees: 1 product
```

---

## 🔄 Vendor Product Lifecycle

```
┌─────────────────┐
│ Vendor Dashboard│
└────────┬────────┘
         │
         ▼
    ┌──────────┐
    │ Click    │
    │ "Add     │
    │ Product" │
    └────┬─────┘
         │
         ▼
┌──────────────────────────┐
│ /vendor/products/create  │
│ (Form)                   │
└────────┬─────────────────┘
         │
         ▼ Vendor fills form
         │ & submits
         │
         ▼
┌───────────────────────────────────┐
│ POST /vendor/products             │
│ VendorProductController@store()   │
└────────┬────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ Validation                           │
│ - Check: All required fields filled  │
│ - Check: Image uploaded (optional)   │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ Auto-assign vendor_id                │
│ $data['vendor_id'] = auth()->user()->id
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ Process image upload                 │
│ Save to: public/img/products/        │
│ Store path in: image_url field       │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ Product::create($data)               │
│ INSERT INTO products...              │
│ VALUES (name, price, vendor_id=10..) │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ Redirect to /vendor/products         │
│ Display: "Product created!"          │
└──────────────────────────────────────┘
```

---

## 🛡️ Cross-Vendor Access Prevention

```
┌─────────────────────────────────────────┐
│ Scenario: Vendor A tries to edit        │
│ Vendor B's Product                      │
└──────────────┬──────────────────────────┘
               │
               ▼
┌────────────────────────────────────────┐
│ GET /vendor/products/999/edit          │
│ (Product 999 belongs to Vendor B)      │
└──────────────┬─────────────────────────┘
               │
               ▼
┌────────────────────────────────────────┐
│ Route Middleware: role:vendor ✅       │
│ Vendor A passes auth check             │
└──────────────┬─────────────────────────┘
               │
               ▼
┌────────────────────────────────────────┐
│ ProductController::edit($product)      │
│ $this->authorize('update', $product)  │
└──────────────┬─────────────────────────┘
               │
               ▼
┌────────────────────────────────────────┐
│ Policy Check:                          │
│ ProductPolicy::update()                │
│                                        │
│ return $product->vendor_id ===         │
│        $user->id;                      │
│                                        │
│ 999->vendor_id = 11 (Vendor B)        │
│ $user->id = 10 (Vendor A)             │
│                                        │
│ Result: 11 !== 10 ❌ FALSE            │
└──────────────┬─────────────────────────┘
               │
               ▼
┌────────────────────────────────────────┐
│ ThrowAuthorizationException            │
│ HTTP 403 Forbidden                     │
│ "This action is unauthorized"          │
└────────────────────────────────────────┘
```

---

## 📍 Route Mapping

```
┌─────────────────────────────────────────────────────────────┐
│                    VENDOR ROUTES MAP                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│ /vendor                                                      │
│   ├─ GET    /          → DashboardController@index          │
│   │                                                          │
│   ├─ /products                                               │
│   │   ├─ GET    /                → ProductController@index   │
│   │   ├─ GET    /create          → ProductController@create  │
│   │   ├─ POST   /                → ProductController@store   │
│   │   ├─ GET    /{id}/edit       → ProductController@edit    │
│   │   ├─ PUT    /{id}            → ProductController@update  │
│   │   ├─ DELETE /{id}            → ProductController@destroy │
│   │   └─ GET    /{id}            → ProductController@show    │
│   │                                                          │
│   └─ /orders                                                 │
│       ├─ GET    /                → OrderController@index     │
│       ├─ GET    /{id}            → OrderController@show      │
│       └─ PUT    /{id}/status     → OrderController@updateStatus
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔍 Query Filtering with VendorScope

```
Vendor 10 executes:
Product::all()

↓ (automatically)

SELECT * FROM products
WHERE vendor_id = 10

Result:
[
  Product { id: 1, name: "Product A", vendor_id: 10 },
  Product { id: 2, name: "Product B", vendor_id: 10 }
]

---

Admin executes:
Product::all()

↓ (no scope applied - is admin)

SELECT * FROM products

Result:
[
  Product { id: 1, name: "Product A", vendor_id: 10 },
  Product { id: 2, name: "Product B", vendor_id: 10 },
  Product { id: 3, name: "Product C", vendor_id: 11 },
  Product { id: 4, name: "Product D", vendor_id: 11 },
  Product { id: 5, name: "Product E", vendor_id: 12 }
]
```

---

## 👥 User Roles & Capabilities

```
┌──────────────┬────────────┬──────────────┐
│ Role         │ Role ID    │ Portal       │
├──────────────┼────────────┼──────────────┤
│ Admin        │ 1          │ /admin/*     │
│ Staff        │ 2          │ /admin/*     │
│ Customer     │ 3          │ /            │
│ Vendor       │ 4          │ /vendor/*    │
└──────────────┴────────────┴──────────────┘

Access Matrix:
         /     /admin/*   /vendor/*
Admin    ✅    ✅         ❌
Staff    ✅    ✅         ❌
Vendor   ✅    ❌         ✅
Customer ✅    ❌         ❌
```

---

## 🧮 Database Schema Reference

```
users table:
┌─────────┬─────────────┬────────────┬─────────┐
│ id      │ name        │ role_id    │ email   │
├─────────┼─────────────┼────────────┼─────────┤
│ 10      │ Vendor A    │ 4          │ v@a.com │
│ 11      │ Vendor B    │ 4          │ v@b.com │
│ 1       │ Admin       │ 1          │ a@a.com │
└─────────┴─────────────┴────────────┴─────────┘

products table:
┌────┬─────────────┬───────────┬────────────┐
│ id │ name        │ vendor_id │ price      │
├────┼─────────────┼───────────┼────────────┤
│ 1  │ Product A   │ 10        │ 100.00     │
│ 2  │ Product B   │ 10        │ 150.00     │
│ 3  │ Product C   │ 11        │ 200.00     │
└────┴─────────────┴───────────┴────────────┘

orders table:
┌────┬─────────┬──────────────┬─────────┐
│ id │ user_id │ order_status │ total   │
├────┼─────────┼──────────────┼─────────┤
│ 1  │ 20      │ pending      │ 250.00  │
│ 2  │ 21      │ shipped      │ 200.00  │
└────┴─────────┴──────────────┴─────────┘

order_items table:
┌────┬──────────┬────────────┬──────────┐
│ id │ order_id │ product_id │ quantity │
├────┼──────────┼────────────┼──────────┤
│ 1  │ 1        │ 1          │ 2        │
│ 2  │ 1        │ 3          │ 1        │
│ 3  │ 2        │ 3          │ 1        │
└────┴──────────┴────────────┴──────────┘
```

---

## 🎯 Vendor Order Visibility

```
Order 1:
├─ OrderItem: Product 1 (vendor_id = 10)
├─ OrderItem: Product 3 (vendor_id = 11)

Visible to:
✅ Admin (can see all orders)
✅ Vendor 10 (has product 1)
✅ Vendor 11 (has product 3)
❌ Vendor 12 (doesn't have any products in this order)

Query for Vendor 10:
Order::whereHas('orderItems.product', function($q) {
    $q->where('vendor_id', 10);
})
→ Returns Order 1
```

---

## 🔗 Controller-Model-View Flow

```
┌──────────────────────────────────────────┐
│ HTTP Request: GET /vendor/products/1/edit │
└────────────────┬─────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│ VendorProductController::edit($product)           │
│                                                   │
│ 1. $this->authorize('update', $product)          │
│    ↓ Check ProductPolicy                         │
│    ↓ If vendor_id != auth()->user()->id → 403   │
│                                                   │
│ 2. $categories = Category::all()                 │
│    ↓ Fetch categories for dropdown               │
│                                                   │
│ 3. return view('vendor.products.edit',           │
│             compact('product', 'categories'))     │
└────────────────┬─────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│ vendor/products/edit.blade.php                    │
│                                                   │
│ @extends('layouts.vendor')                       │
│ ├─ Extends vendor layout                         │
│ ├─ Uses vendor sidebar                           │
│ ├─ Uses vendor navbar                            │
│ └─ Renders edit form                             │
│     ├─ Product name field                        │
│     ├─ Price field                               │
│     ├─ Stock field                               │
│     ├─ Category dropdown                         │
│     └─ Submit button                             │
└────────────────┬─────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│ HTML Response sent to browser                     │
└────────────────────────────────────────────────────┘
```

---

## 📈 Vendor Growth Path

```
Week 1: New Vendor
├─ Create account (role_id = 4)
├─ Access /vendor
├─ Create first product
└─ Enable selling

Week 2: Growing
├─ Add more products
├─ View orders
├─ Update order status
└─ Monitor stock

Week 4: Scaling
├─ Manage multiple products
├─ Handle high order volume
├─ Track inventory
└─ Update pricing

Future:
├─ Vendor analytics
├─ Sales reports
├─ Payout management
└─ Performance metrics
```

---

## 🚀 Deployment Checklist

```
Pre-Deployment:
☐ All tests passing
☐ No syntax errors
☐ Configuration verified
☐ Database migrations ready

Deployment:
☐ Pull latest code
☐ Run migrations
☐ Clear caches
☐ Verify routes
☐ Test logins

Post-Deployment:
☐ Monitor logs
☐ Test vendor access
☐ Verify data isolation
☐ Performance check
☐ User feedback
```

---

**Visual Guide Version**: 1.0  
**Last Updated**: January 24, 2026
